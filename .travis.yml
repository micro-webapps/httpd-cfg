language: cpp
compiler:
  - gcc
install:
  - sudo add-apt-repository ppa:apache-helpdesk/httpd-ppa -y
  - sudo apt-get -qq update
  - sudo apt-get -qq install python3 apache2
script:
  - ./runtests
branches:
  only:
    - master
notifications:
  #irc:
    #channels:
      #- "chat.freenode.net#mwa"
    #template:
      #- "%{repository}/%{branch} (%{commit} - %{author}): %{message}"
  email:
    recipients:
      - jkaluza@redhat.com
    on_success: always
    on_failure: always
